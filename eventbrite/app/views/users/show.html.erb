<p id="notice"><%= notice %></p>

<h1>Page Profil</h1>

<p>Bienvenue sur la page d'ajout d'un avatar pour le User portant l'id : <%=@user.id%></p>
<h3>Avatar actuel</h3>
<%if @user.avatar.attached?%>
  <%= image_tag @user.avatar, alt: 'avatar' %>
<%else%>
  <p>=== Il n'y a pas encore d'avatar lié à cet utilisateur ===</p>
<%end%>

<h3>Changer d'avatar ?</h3>
<%= form_tag user_avatars_path(@user), multipart: true do %>
  <%= file_field_tag :avatar %>
  <%= submit_tag "mettre à jour" %>
<% end %>

<p>
  <strong>First name:</strong>
  <%= @user.first_name %>
</p>

<p>
  <strong>Last name:</strong>
  <%= @user.last_name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Description:</strong>
  <%= @user.description %>
</p>

<% if params[:id] == current_user.id.to_s %>
	<%= link_to 'Edit', edit_user_registration_path %> 
<% end %>
<%= link_to 'Back', :back %>

<% if @liseve.length != 0 %>
	<br/>
	<br/>
	<table>
		<tr><td><b>Date &nbsp;</b></td><td><b>Title</b></td></tr>
		<% @liseve.each do |uneve| %>
			<tr><td nowrap><b><%= link_to uneve.start_date.to_s[0,16], event_path(uneve) %> &nbsp;</b></td><td><%= uneve.title %></td></tr>
		<% end %>
	</table>
<% end %>
